<p id="notice"><%= notice %></p>

<h1>Categories</h1>
  <tbody>
    <% @categories.each do |category| %>
      <ul>
        <li><%= link_to category.title, category %></li>
      </ul>
    <% end %>
  </tbody>
</table>
