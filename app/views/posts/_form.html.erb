<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :text %>
  <%= f.text_area :text %>

  <%= f.select :category_id, Category.all.map {|c| [c.title, c.id]} %>

  <%= f.submit%>
<% end %>
